version: '3.8'

services:
  asteroid-game:
    image: asteroid-game:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    container_name: asteroid-game-container